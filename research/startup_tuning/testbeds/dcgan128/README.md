# Plain DCGAN control

This is the current training formulation with a DCGAN generator and a DCGAN
discriminator. There is no DINOv3 critic and no TransGAN generator. The logos
manifest, batch, seeds, logistic relativistic loss, lazy b-cap penalty, prior,
and Adam rates match the TransGAN/DINO runs.

It has not been started. The fresh run directory is
`~/dev/hypergan/training-runs/train-dcgan-128-formulation`. It does not resume
`train-dcgan-128`.

```bash
~/dev/hypergan/training-runs/start-dcgan-128-formulation.sh
```
