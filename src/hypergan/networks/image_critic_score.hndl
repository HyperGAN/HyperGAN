# Pixel score plus three equally weighted feature scores.
pixel, features = split(1)
features = sum(features, 1, keepdim=True)
features = scale(features, 0.5773502691896258)
score = add(pixel, features)
scale(score, 0.7071067811865475)
